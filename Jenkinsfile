pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'sudo docker build -t ttt .'
            }
        }
        stage('Deploy') {
            steps {
               sh 'echo comming soon'
            }
        }
    }
}
